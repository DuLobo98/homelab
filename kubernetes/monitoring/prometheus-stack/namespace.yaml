apiVersion: v1
kind: Namespace
metadata:
  name: prometheus-stack
  labels:
    # node-exporter requires host namespaces, host paths, and a host port.
    pod-security.kubernetes.io/enforce: privileged
